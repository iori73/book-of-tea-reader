{
  "name": "book-of-tea-reader",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node src/build.mjs",
    "serve": "npm run build && python3 -m http.server 8080 --directory dist"
  }
}
